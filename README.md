# センサー工作の技術解説ページ

100円ショップの300円リモコンライトを分解して赤外線センサーを取り出し、そこから
**マイコン2台とPCをつないで、ゲームで目標スコアを取るとガチャガチャのロックが開く**
仕組みを作るまでを、一連の技術記事としてまとめた1ページのサイトです。
展示ブースの前でスマホから読める前提で作っています。

## 内容

1. **まずは、動いているところを見てください(デモ動画)**
2. 100円ショップで見つけた「300円のセンサー」
3. 分解してみると、こうなっていた(注釈つき写真)
4. ここをマイコンにつなぐと、受信できる(配線図)
5. 実際に受信してみる:シリアルモニタと受信コードの対応表
6. そもそも、ESP32ってなに?
7. **ここから本題:ゲームでガチャのロックを開ける(全体構成図)**
8. **ロックの正体は「サーボモーター」(PWMで角度を伝える仕組み)**
9. **「開けろ」をどうやって伝えるか(BLEのキャラクタリスティック / WiFi + HTTP + mDNS)**
10. **「カプセルが出た」をどう確かめるか(反射型IRセンサー / 受信専用との違い)**
11. **いちばん面白いところ:状態を管理する(ステートマシン・誤検知対策)**
12. 赤外線・Bluetooth・WiFiの使い分け
13. アンケート
14. (おまけ)他にもこんなセンサーがある
15. (おまけ)まとめ

### 無線の構成について(記事の内容と実装の対応)

区間ごとに使う無線が違います。記事もこの通りに書いてあります。

| 区間 | 方式 | 対応する実装 |
| --- | --- | --- |
| リモコン → ESP32① | 赤外線 | `firmware/color_memory_game_ble`(受信部) |
| ESP32① → PC | Bluetooth(BLE) | `firmware/color_memory_game_ble` |
| PC ⇄ ESP32②(ガチャ側) | WiFi(HTTP + mDNS) | `firmware/gacha_lock_wifi` |

ガチャ側は当初BLEでしたが、電波が弱いと接続が切れて「排出した」の通知を取りこぼす問題が
実イベントで出たため、ポーリングで追いつけるWiFi(`/status` を0.4秒ごとに取得)へ移行しました。
記事のセクション09・12はこの経緯をそのまま説明の題材にしています。

ガチャ本体のロック機構(3Dプリンタ設計)は共同制作者による設計です。

※ページ内には来場者向けのソースコード公開リンクは置いていません(公開の導線は付けない方針)。

## 撮影素材(差し込み済み)

2026-08-01 に撮影した素材を反映済みで、**プレースホルダ枠は全て埋まっています**。

| 素材 | ファイル | 場所 |
| --- | --- | --- |
| デモ動画 | `videos/demo.mp4` | セクション01 |
| サーボとロック機構 | `images/gacha-servo-lock.jpg` | セクション08 |
| ガチャ側ESP32(正面/裏側の2枚) | `images/gacha-display.jpg` / `images/gacha-esp32.jpg` | セクション09(横並び) |
| 排出口の反射型IRセンサー | `images/gacha-ir-sensor.jpg` | セクション10 |

**元データ(4K動画・4032pxの写真)はリポジトリに入れていません。**
`画像/opencampus素材/`(親フォルダ側、git管理外)に退避してあります。
Web用に「トリミング → 長辺1600pxに縮小 → JPEG品質84」で書き出したものだけをコミットしています。
撮り方のメモは [`SHOOTING_LIST.md`](SHOOTING_LIST.md)、動画の変換手順は [`videos/README.md`](videos/README.md) に。

### まだ撮っていない(任意)

- ガチャ機全体の写真(セクション07の冒頭に置くと「これを開けるんだ」が一目で伝わる)
- 展示ブースの設置全景

## 構成

素のHTML + CSS(フレームワークなし、ビルド不要)。`index.html` を開くだけで表示できます。

```
opencampus/
├── index.html
├── style.css
├── images/             写真(分解写真・配線写真など)
│   └── sensors/         スターターキットのパーツ紹介用の写真
├── videos/             デモ動画(demo.mp4 を置く)
├── POSTER_PROMPT.md    ガチャ機の横に貼るA4ポスターの生成プロンプト
└── README.md
```

図はすべてインラインSVGで自作しています。狭い画面では文字が読めなくなるため、
`.diagram-scroll` で囲んで **700px以下では縮小を止めて横スクロール**に切り替えています。

## 公開

Vercel で公開しています。`main` への push で自動デプロイされます。

## アンケート

来場者アンケートは Google フォームへのリンクです。回答は Google スプレッドシートに集計されます。
